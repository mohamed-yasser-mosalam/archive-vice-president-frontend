import { ShowAllImportantFile } from './show-all-important-file';

describe('ShowAllImportantFile', () => {
  it('should create an instance', () => {
    expect(new ShowAllImportantFile()).toBeTruthy();
  });
});
