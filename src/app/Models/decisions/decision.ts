export class Decision {
  num:number;
  summary:string;
  qarar:string

  constructor(num: number, summary: string,qarar:string) {
    this.num = num;
    this.summary = summary;
    this.qarar;
  }
}
