import { ShowTodayExports } from './show-today-exports';

describe('ShowTodayExports', () => {
  it('should create an instance', () => {
    // @ts-ignore
    expect(new ShowTodayExports()).toBeTruthy();
  });
});
