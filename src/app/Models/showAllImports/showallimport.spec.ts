import { Showallimport } from './showallimport';

describe('Showallimport', () => {
  it('should create an instance', () => {
    // @ts-ignore
    expect(new Showallimport()).toBeTruthy();
  });
});
