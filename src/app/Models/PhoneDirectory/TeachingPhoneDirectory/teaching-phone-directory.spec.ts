import { TeachingPhoneDirectory } from './teaching-phone-directory';

describe('TeachingPhoneDirectory', () => {
  it('should create an instance', () => {
    expect(new TeachingPhoneDirectory()).toBeTruthy();
  });
});
