import { ShowTodayImports } from './show-today-imports';

describe('ShowTodayImports', () => {
  it('should create an instance', () => {
    // @ts-ignore
    expect(new ShowTodayImports()).toBeTruthy();
  });
});
