import { FacultyPhoneDirectory } from './faculty-phone-directory';

describe('FacultyPhoneDirectory', () => {
  it('should create an instance', () => {
    expect(new FacultyPhoneDirectory()).toBeTruthy();
  });
});
